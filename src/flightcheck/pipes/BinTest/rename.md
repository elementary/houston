Rename binary to match RDNN

AppCenter has noticed you ship a file in `/usr/bin`, but it's not named
`{{ data }}`. Please ensure that your binary is named `{{ data }}`
and is installed to `/usr/bin`.
