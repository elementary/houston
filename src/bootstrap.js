/**
 * houston/src/bootstrap.ts
 * A bootstrap file that should be loaded once in the whole application.
 * Used for loading polyfills and other _hacks_ as needed.
 * Because this file is bootstraping the env, it must be es5 styled. Sorry.
 */

require('reflect-metadata')
