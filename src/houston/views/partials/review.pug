a(href="project/" + project.name.domain + "/review/no")
  div.fab.error
    i.fa.fa-times
    div.popover-container
      div.popover-content
        h3 Request Changes
        p #{project.name.human} needs fixing

a(href="project/" + project.name.domain + "/review/yes")
  div.fab.success
    i.fa.fa-check
    div.popover-container
      div.popover-content
        h3 Approve
        p Release #{project.name.human} into AppCenter
