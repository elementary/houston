---

sudo: required

language: node_js

git:
  depth: 1

before_install:
  - docker pull mongo

script:
  - npm run build
  - npm run lint
  - npm run test

services:
  - docker
  - mongodb

addons:
  apt:
    sources:
      - mongodb-3.0-precise
    packages:
      - mongodb-org-server
